vscode:
  extensions:
    - tabnine.tabnine-vscode 

tasks: 
  - name: Persist Tabnine
    init: |
      mkdir ~/.config -p
      mkdir .tabnine/config -p
      ln -s .tabnine/config ~/.config/TabNine
      
      mkdir ~/.local/share -p
      mkdir .tabnine/.local/share -p
      ln -s .tabnine/share ~/.local/share/TabNine

ports:
  - port: 5555
    onOpen: ignore

